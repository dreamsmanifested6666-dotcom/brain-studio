# Brain Studio backend — runtime dependencies.
#
# Web framework + serialization
fastapi>=0.115,<0.120
uvicorn[standard]>=0.32,<0.40
pydantic>=2.9,<3
python-dotenv>=1.0,<2

# Embedding-baseline engine (the public deployment runs on this).
# fastembed uses ONNX runtime — no torch needed, ~130 MB model, fast
# cold-start on free-tier servers.
fastembed>=0.4,<0.8
onnxruntime>=1.18,<2

# General math + config
numpy>=1.26,<3
pyyaml>=6.0,<7

# Optional — only required when TRIBE_ENGINE=real (Meta's gated stack):
#   torch>=2.4
#   transformers>=4.45
#   nilearn>=0.10
#   nibabel>=5.2
# Comment out for the serverless / free-tier deployment.
